<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Notification Channels
    |--------------------------------------------------------------------------
    */

    'default' => env('NOTIFICATION_MAIL_DRIVER', 'mail'),

    /*
    |--------------------------------------------------------------------------
    | Push notifications: single recipient
    |--------------------------------------------------------------------------
    | Email пользователя, который получает пуш (выезд из гаража и т.д.).
    | Если не задан — пуш не отправляется никому.
    */
    'push_recipient_email' => env('PUSH_RECIPIENT_EMAIL', 'rvr@arguss.lv'),

    /*
    |--------------------------------------------------------------------------
    | Notification Channels
    |--------------------------------------------------------------------------
    */

    'channels' => [
        'mail' => [
            'driver' => 'mail',
        ],

        'database' => [
            'driver' => 'database',
            'table' => 'notifications',
        ],

        // Добавляем WebPush
        'webpush' => [
            'driver' => 'webpush',
        ],
    ],

];
